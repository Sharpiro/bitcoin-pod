services:
  debugger:
    image: curlimages/curl
    user: "2000"
    command: cat /home/bitcoin_pod_user/control_cookie/cookie
    depends_on:
      - "tor"
    profiles:
      - debug
    volumes:
      - tor_cookie:/home/bitcoin_pod_user/control_cookie
