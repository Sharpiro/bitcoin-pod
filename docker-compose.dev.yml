services:
  debugger:
    image: curlimages/curl
    user: "2000"
    command: ls -la /home/bitcoin_pod_user
    depends_on:
      - "tor"
    profiles:
      - disabled
    volumes:
      - tor_cookie:/home/bitcoin_pod_user/.tor
    networks:
      - primary
